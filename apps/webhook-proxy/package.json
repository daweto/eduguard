{
  "name": "webhook-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "lint": "eslint .",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.10.3"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "wrangler": "^4.45.0",
    "typescript": "^5.9.3"
  }
}
