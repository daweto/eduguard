# Cloudflare Workers Environment Variables
# These are typically set via wrangler.jsonc or Cloudflare dashboard

# AWS Rekognition (for future integration)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1
# AWS_REKOGNITION_COLLECTION=eduguard-school-12345

# Database (configured in wrangler.jsonc)
# DB binding name: DB
# R2 binding name: PHOTOS

# For local development, bindings are configured in wrangler.jsonc:
# - d1_databases[0].binding = "DB"
# - r2_buckets[0].binding = "PHOTOS"

# Note: Cloudflare Workers use bindings instead of traditional environment variables
# See wrangler.jsonc for D1 and R2 configuration
